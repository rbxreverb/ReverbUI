# ReverbLib Documentation

ReverbLib currently keeps the Rayfield-style API so existing examples can be adapted by replacing the loaded variable with `ReverbLib`.

Start with `README.md` and `example.lua` in this repo. More complete custom documentation can be written once the first hosted version is confirmed working.

Original upstream documentation: https://docs.sirius.menu/rayfield
